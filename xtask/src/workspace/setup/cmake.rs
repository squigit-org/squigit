use crate::{Runtime, XtaskResult};

pub fn run(runtime: &Runtime) -> XtaskResult {
    /**************************
    TYPE REAL LOGIC HERE

    Verify or install CMake and validate the native build generator.
    **************************/

    runtime.success("[mock] setup cmake: ready");
    Ok(())
}
