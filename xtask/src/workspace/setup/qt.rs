use crate::{Runtime, XtaskResult};

pub fn run(runtime: &Runtime) -> XtaskResult {
    /**************************
    TYPE REAL LOGIC HERE

    Verify or install Qt 6 and qmake for native capture builds.
    **************************/

    runtime.success("[mock] setup qt: ready");
    Ok(())
}
