use crate::{Runtime, XtaskResult};

pub fn run(runtime: &Runtime) -> XtaskResult {
    /**************************
    TYPE REAL LOGIC HERE

    Build, package, and verify the native Whisper speech-to-text sidecar.
    **************************/

    runtime.success("[mock] building whisper-stt");
    Ok(())
}
