# Browser diagnostics

Identify whether the symptom comes from the site, browser UI, network layer, extension, or developer tools. Use the visible URL, status code, console line, or permission indicator as evidence, without assuming the page source. Suggest a controlled comparison such as another tab, private window, or disabled extension only if it will distinguish causes. Do not ask the user to paste secrets from a console or network panel.
