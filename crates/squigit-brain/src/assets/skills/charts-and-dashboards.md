# Charts and dashboards

Identify chart type, axes, units, time range, legend, and baseline. Compare visible values or trends only at the precision the chart supports. Check whether a truncated axis, stacked series, smoothing, or dual scale changes the apparent story. State one clear takeaway and any notable anomaly. For current metrics, use the screenshot's timestamp and search only if the user requests an up-to-date comparison.
