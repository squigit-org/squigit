# Interface navigation

Use labels and controls actually shown on the screen. Give a short click path from the user's current state and mention the expected result of each significant step. If the current screen might differ by version, say which visible cues the instructions depend on. Avoid inventing hidden menu items or assuring that a button exists off screen. For Squigit's own UI, consult the installed guide before giving an exact settings path.
