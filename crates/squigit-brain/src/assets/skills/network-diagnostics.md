# Network diagnostics

Separate local connectivity, name resolution, TLS certificate, proxy/VPN, authentication, rate limiting, and remote service failure. A browser's generic offline page does not identify which layer failed. Propose one observation per layer and interpret possible outcomes. Do not recommend disabling certificate validation or a firewall as a casual fix. Check current service status through the web only when the user asks and the service is identifiable.
