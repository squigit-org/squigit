# Windows diagnostics

Identify whether the screen shows Windows Settings, Explorer, PowerShell, Command Prompt, Device Manager, or an app before naming controls or commands. Prefer built-in inspection steps and state the expected output. Distinguish Windows 10 from Windows 11 only when visible or supplied. Be careful with registry edits, service changes, driver removal, and administrator commands; explain their effect and a safer diagnostic first.
