[package]
name = "svc-speech-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tokio = { version = "1.0", features = ["process", "io-util", "sync", "rt", "macros"] }
log = "0.4"
anyhow = "1.0"