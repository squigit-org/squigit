# Copyright 2026 a7mddra
# SPDX-License-Identifier: Apache-2.0

# Runtime dependencies for PP3 OCR inference.
# Keep this list minimal and add back only hard requirements discovered by smoke checks.

requests
httpx
PyYAML==6.0.2
numpy>=1.24
protobuf>=3.20.2
Pillow
opt-einsum==3.3.0
networkx
typing-extensions
safetensors>=0.6.0
setuptools; python_version >= "3.12"

# PaddleX core/runtime path used by PaddleOCR inference.
aistudio-sdk>=0.3.5
chardet==5.2.0
colorlog
filelock
packaging
prettytable
py-cpuinfo
pydantic>=2
ruamel.yaml
ujson
tqdm
imagesize
pyclipper
python-bidi
shapely
psutil
pandas>=1.3

# Reduced OpenCV footprint versus contrib build.
opencv-python-headless==4.10.0.84
