[workspace]
members = [
    # "archive/desktop",  # Frozen at v0.1.1

    "crates/*",
    "sidecars/qt-capture",
    "xtask",
]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2021"

[profile.release]
lto = true
codegen-units = 1
opt-level = "z"
strip = "debuginfo"
panic = "abort"
