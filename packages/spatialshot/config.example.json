{
  "__comment_block": {
    "@file": "config.example.json",
    "@brief": "Configuration template for application secrets and environment-specific variables.",
    "@details": [
      "This file serves as a template for the application's private configuration.",
      "To enable the application, you must perform the following steps:",
      "1. Create a copy of this file in the same directory.",
      "2. Rename the copy to 'config.private.json'.",
      "3. Populate the fields in the new file with your specific credentials.",
      "The 'config.private.json' file is listed in .gitignore and MUST NOT be committed to version control to prevent leaking sensitive information."
    ],
    "@note": "Do NOT modify this template directly. Only edit your local 'config.private.json' file."
  },
  "google_oauth": {
    "__comment": "Configuration for Google OAuth 2.0. Credentials can be obtained from the Google Cloud Console under 'APIs & Services' > 'Credentials'.",
    "client_id": "YOUR_CLIENT_ID_HERE",
    "client_secret": "YOUR_CLIENT_SECRET_HERE",
    "project_id": "YOUR_GCP_PROJECT_ID_HERE",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_endpoint": "https://oauth2.googleapis.com/token",
    "provider_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "redirect_uris": [
      "http://localhost:3000/auth/google/callback"
    ]
  },
  "google_gemini": {
    "__comment": "Configuration for Google Gemini API. Credentials can be obtained from the Google Cloud Console under 'APIs & Services' > 'Credentials'.",
    "api_key": "YOUR_GEMINI_API_KEY_HERE",
    "api_endpoint": "YOUR_GEMINI_API_ENDPOINT_HERE",
    "project_name": "YOUR_GCP_PROJECT_NAME_HERE",
    "project_number": "YOUR_GCP_PROJECT_NUMBER_HERE"
  }
}
