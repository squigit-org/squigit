name: "OCR Build Matrix"
run-name: OCR Runtime Build ${{ inputs.request_id || github.run_id }}

on:
  workflow_dispatch:
    inputs:
      request_id:
        description: "Optional caller correlation ID"
        required: false
        default: ""
        type: string
      ocr_version:
        description: "Native OCR version expected from the built executable"
        required: false
        default: "0.1.1"
        type: string

jobs:
  build-ocr:
    name: OCR ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu x64
            slug: ubuntu-x64
            runner: ubuntu-latest
            container: ""
            target: x86_64-unknown-linux-gnu
          - name: Fedora x64
            slug: fedora-x64
            runner: ubuntu-latest
            container: fedora:latest
            target: x86_64-unknown-linux-gnu
          - name: macOS ARM
            slug: macos-arm
            runner: macos-15
            container: ""
            target: aarch64-apple-darwin
          - name: Windows x64
            slug: windows-x64
            runner: windows-latest
            container: ""
            target: x86_64-pc-windows-msvc
    env:
      OCR_OS_NAME: ${{ matrix.name }}
      OCR_TARGET: ${{ matrix.target }}

    steps:
      - name: Pre-setup Fedora container
        if: matrix.slug == 'fedora-x64'
        run: |
          dnf install -y git curl tar gzip gcc-c++ make python3 python3-pip perl-open perl-FindBin findutils patch

      - uses: actions/checkout@v6

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build OCR baseline (onefile)
        uses: ./.github/actions/build-squigit-ocr
        with:
          python-version: "3.12"
          pyi-mode: "onefile"

      - name: Clean OCR build folders only
        if: runner.os != 'Windows'
        run: |
          rm -rf squigit-ocr/native/build squigit-ocr/native/dist

      - name: Clean OCR build folders only (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Remove-Item -Recurse -Force squigit-ocr/native/build, squigit-ocr/native/dist -ErrorAction SilentlyContinue

      - name: Build OCR candidate (onedir)
        id: final-build
        uses: ./.github/actions/build-squigit-ocr
        with:
          python-version: "3.12"
          pyi-mode: "onedir"
          measure-runtime: "true"
          create-windows-archive: "true"
          windows-asset-name: "squigit-ocr-win-x64.zip"

      - name: Validate native OCR version (Unix)
        if: runner.os != 'Windows'
        shell: bash
        env:
          EXPECTED_VERSION: ${{ inputs.ocr_version }}
        run: |
          set -euo pipefail
          executable="binaries/paddle-ocr-${OCR_TARGET}/squigit-ocr"
          chmod +x "${executable}" || true
          actual_version="$("${executable}" --version | tr -d '\r' | tail -n 1)"
          if [ "${actual_version}" != "${EXPECTED_VERSION}" ]; then
            echo "OCR version mismatch: built=${actual_version} expected=${EXPECTED_VERSION}" >&2
            exit 1
          fi

      - name: Validate native OCR version (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          EXPECTED_VERSION: ${{ inputs.ocr_version }}
        run: |
          $ErrorActionPreference = "Stop"
          $actualVersion = (& "binaries/paddle-ocr-$env:OCR_TARGET/squigit-ocr.exe" --version | Select-Object -Last 1).Trim()
          if ($actualVersion -ne $env:EXPECTED_VERSION) {
            throw "OCR version mismatch: built=$actualVersion expected=$env:EXPECTED_VERSION"
          }

      - name: Generate PNG smoke image (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          squigit-ocr/native/venv/bin/python - <<'PY'
          import os
          from pathlib import Path
          from PIL import Image, ImageDraw

          source = Image.new("RGB", (240, 60), "white")
          text = f"SQUIGIT {os.environ['OCR_OS_NAME']} OCR SMOKE"
          ImageDraw.Draw(source).text((10, 20), text, fill="black")
          image = source.resize((960, 240), Image.Resampling.NEAREST)
          image.save(Path("/tmp/ocr-smoke.png"))
          PY

      - name: Generate PNG smoke image (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $code = @"
          from pathlib import Path
          import os
          from PIL import Image, ImageDraw

          source = Image.new('RGB', (240, 60), 'white')
          text = f"SQUIGIT {os.environ['OCR_OS_NAME']} OCR SMOKE"
          ImageDraw.Draw(source).text((10, 20), text, fill='black')
          image = source.resize((960, 240), Image.Resampling.NEAREST)
          image.save(Path(os.environ['TEMP']) / 'ocr-smoke.png')
          "@
          & squigit-ocr/native/venv/Scripts/python.exe -c $code

      - name: Run OCR executable and print JSON (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          chmod +x "binaries/paddle-ocr-$OCR_TARGET/squigit-ocr" || true
          "binaries/paddle-ocr-$OCR_TARGET/squigit-ocr" /tmp/ocr-smoke.png | tee /tmp/ocr-smoke-output.json

      - name: Run OCR executable and print JSON (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          & "binaries/paddle-ocr-$env:OCR_TARGET/squigit-ocr.exe" "$env:TEMP/ocr-smoke.png" | Tee-Object -FilePath "$env:TEMP/ocr-smoke-output.json"

      - name: Validate OCR JSON payload (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          squigit-ocr/native/venv/bin/python - <<'PY'
          import json
          from pathlib import Path

          out = Path("/tmp/ocr-smoke-output.json")
          payload = json.loads(out.read_text(encoding="utf-8").strip())
          if isinstance(payload, dict) and "error" in payload:
              raise SystemExit(f"OCR returned error: {payload['error']}")
          if not isinstance(payload, list):
              raise SystemExit(f"OCR payload type {type(payload).__name__}, expected list")
          if not payload:
              raise SystemExit("OCR payload list is empty")
          text_values = [str(item.get("text", "")).strip() for item in payload if isinstance(item, dict)]
          merged = " | ".join(text for text in text_values if text)
          print(f"Detected entries: {len(payload)}")
          print(f"Detected text: {merged}")
          PY

      - name: Validate OCR JSON payload (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $code = @"
          import json
          import os
          from pathlib import Path

          out = Path(os.environ['TEMP']) / 'ocr-smoke-output.json'
          payload = json.loads(out.read_text(encoding='utf-8').strip())
          if isinstance(payload, dict) and 'error' in payload:
              raise SystemExit(f'OCR returned error: {payload["error"]}')
          if not isinstance(payload, list):
              raise SystemExit(f'OCR payload type {type(payload).__name__}, expected list')
          if not payload:
              raise SystemExit('OCR payload list is empty')
          text_values = [str(item.get('text', '')).strip() for item in payload if isinstance(item, dict)]
          merged = ' | '.join(text for text in text_values if text)
          print(f'Detected entries: {len(payload)}')
          print(f'Detected text: {merged}')
          "@
          & squigit-ocr/native/venv/Scripts/python.exe -c $code

      - name: Create runtime archive (Unix)
        if: runner.os != 'Windows'
        shell: bash
        env:
          RUNTIME_DIR: binaries/paddle-ocr-${{ matrix.target }}
          PLATFORM: ${{ matrix.slug }}
        run: |
          set -euo pipefail
          mkdir -p target/ocr-transfer

          case "${PLATFORM}" in
            macos-arm) asset_name="squigit-ocr-mac-aarch64.tar.gz" ;;
            ubuntu-x64) asset_name="squigit-ocr-ubuntu-x64.tar.gz" ;;
            fedora-x64) asset_name="squigit-ocr-fedora-x64.tar.gz" ;;
            *) echo "Unsupported Unix OCR platform: ${PLATFORM}" >&2; exit 1 ;;
          esac

          asset_path="target/ocr-transfer/${asset_name}"
          tar -czf "${asset_path}" -C "${RUNTIME_DIR}" .
          cp "target/ocr-size/ocr-size-${OCR_TARGET}.json" target/ocr-transfer/

      - name: Stage runtime archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path target/ocr-transfer -Force | Out-Null
          Copy-Item -LiteralPath "${{ steps.final-build.outputs.asset-path }}" -Destination target/ocr-transfer/
          Copy-Item -LiteralPath "target/ocr-size/ocr-size-$env:OCR_TARGET.json" -Destination target/ocr-transfer/

      - name: Upload packaged OCR runtime
        uses: actions/upload-artifact@v7
        with:
          name: squigit-ocr-runtime-${{ matrix.slug }}
          compression-level: 0
          if-no-files-found: error
          path: target/ocr-transfer/
