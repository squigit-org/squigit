name: "Build Squigit OCR CLI"
description: "Builds the Python-based Squigit OCR standalone executable"
inputs:
  python-version:
    description: "Python version used to build the OCR sidecar"
    required: false
    default: "3.13"
  pyi-mode:
    description: "PyInstaller mode: onedir (default) or onefile (baseline measurement)"
    required: false
    default: "onedir"
  create-windows-archive:
    description: "When true on Windows, zip the OCR runtime directory and expose artifact outputs"
    required: false
    default: "false"
  windows-runtime-dir:
    description: "OCR runtime directory to zip when create-windows-archive=true"
    required: false
    default: "packaging/binaries/squigit-ocr-x86_64-pc-windows-msvc"
  windows-asset-name:
    description: "ZIP filename to produce when create-windows-archive=true"
    required: false
    default: "squigit-ocr-win-x64.zip"
outputs:
  asset-path:
    description: "Absolute path to generated Windows ZIP asset"
    value: ${{ steps.windows-archive.outputs.asset_path }}
  asset-name:
    description: "Generated Windows ZIP filename"
    value: ${{ steps.windows-archive.outputs.asset_name }}
  asset-sha256:
    description: "SHA256 of generated Windows ZIP asset"
    value: ${{ steps.windows-archive.outputs.asset_sha256 }}
runs:
  using: "composite"
  steps:
    - name: Setup Python
      uses: actions/setup-python@v6
      with:
        python-version: ${{ inputs.python-version }}

    - name: Build Squigit OCR CLI
      shell: bash
      env:
        SQUIGIT_OCR_PYI_MODE: ${{ inputs.pyi-mode }}
      run: |
        cargo xtask build ocr

    - name: Create Windows OCR Archive
      if: runner.os == 'Windows' && inputs.create-windows-archive == 'true'
      id: windows-archive
      shell: pwsh
      env:
        RUNTIME_DIR: ${{ inputs.windows-runtime-dir }}
        ASSET_NAME: ${{ inputs.windows-asset-name }}
      run: |
        $ErrorActionPreference = "Stop"

        if (-not (Test-Path -LiteralPath $env:RUNTIME_DIR)) {
          throw "Windows runtime directory not found: $env:RUNTIME_DIR"
        }

        $runtimeDir = (Resolve-Path -LiteralPath $env:RUNTIME_DIR).Path
        $assetDir = Join-Path $env:RUNNER_TEMP "winget-assets"
        New-Item -ItemType Directory -Path $assetDir -Force | Out-Null

        $assetPath = Join-Path $assetDir $env:ASSET_NAME
        if (Test-Path -LiteralPath $assetPath) {
          Remove-Item -LiteralPath $assetPath -Force
        }

        Compress-Archive -Path (Join-Path $runtimeDir "*") -DestinationPath $assetPath -Force
        $sha = (Get-FileHash -LiteralPath $assetPath -Algorithm SHA256).Hash.ToLowerInvariant()

        "asset_path=$assetPath" >> $env:GITHUB_OUTPUT
        "asset_name=$env:ASSET_NAME" >> $env:GITHUB_OUTPUT
        "asset_sha256=$sha" >> $env:GITHUB_OUTPUT
