#!/bin/bash
# This script is run by the GitHub Actions runner
set -e
echo "--- Building Linux Installer ---"
cd packaging/linux
go build -ldflags="-s -w" -o ../../spatialshot-installer main.go
chmod +x ../../spatialshot-installer
echo "--- Linux Installer Built Successfully ---"
